using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Basic : MonoBehaviour
{
    
    public int i;
    public bool b;
    public float f;
    public string s;
    public int[] a;
    public float[] j;

    /*
    void Start() // 1번만 실행 
    {
        string res = "";
        for(int i=0; i<10; i++)
        {
            if(a[i]%2 == 0)
            {
                res = res + ", " + a[i];
            }
        };
        Debug.Log(res);



      
       int result = 0;
       int[] arr = new int[10];
       //10층짜리 정수형 배열
       for (int i = 0; i < 11; i++)
       {
           result = result + i;
       };


       // 0 - 10 , 1- 9 , ,,, 10 - 0
       //다더해서 출력
       Debug.Log(result);


       //kor eng mat 
       //배열 
       // 반복문 더하기
       int res = 0;
       int kor = 70, eng = 80, mat = 90;
       int[] ar = new int[3];

       ar[0] = kor;
       ar[1] = eng;
       ar[2] = mat;

       for (int i = 0; i < 3; i++){
           res = res + ar[i];
       };

       Debug.Log(res);

       // 정수 홀수인지 짝수인지 판별

       string res;
       int n = 5;
       if(n%2 == 0)
       {
           res = "짝수";  
       }
       else{
           res = "홀수";
       };
       Debug.Log("정수 " + n + "는(은)" + res + "입니다");
       
  

        // 퍼블릭 배열을 선언 int 10개
        // 반복문을 사용해서 짝수인 값을 출력 
        

    }   */

    // Update is called once per frame
    void Update() // 주기적으로 실행 
    {
     
        
    }
}
